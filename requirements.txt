# FastAPI and ASGI server
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36

# Testing (TestClient uses httpx)
pytest==8.3.4
httpx==0.28.1

# Linting
ruff==0.6.9

# Optional: automatic API docs (included with FastAPI)
# Pydantic is a dependency of FastAPI
